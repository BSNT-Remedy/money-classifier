services:
  - type: web
    name: pesoscan
    env: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn core.wsgi:application --log-file -
    envVars:
      - key: DEBUG
        value: 'False'